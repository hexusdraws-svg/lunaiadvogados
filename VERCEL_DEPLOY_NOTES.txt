Vercel deployment configuration:
- TanStack Start runs through Nitro.
- Nitro uses the Vercel preset.
- Build output is written to .vercel/output.
- The root vercel.json explicitly points Vercel to .vercel/output.
- No SPA rewrite is required.
- Local preview remains on TanStack Start's normal server output.